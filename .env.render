# R# === CORE DJANGO SETTINGS ===
SECRET_KEY=%&e$ea5h=idf38c47rh*98dyupkdpb99bzei5ny3tmzzw84^uj^
DEBUG=False
DJANGO_SETTINGS_MODULE=scn_esg_platform.settings

# === HOST CONFIGURATION ===
ALLOWED_HOSTS=.onrender.com,localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=https://your-vercel-app.vercel.app

# === RENDER BUILD CONFIGURATION ===
# Build Command: cd backend && pip install -r requirements.py313.txt && python manage.py collectstatic --noinput && python manage.py migrate
# Start Command: cd backend && gunicorn scn_esg_platform.wsgi:application --bind 0.0.0.0:$PORT

# === DATABASE (SQLite - No external database needed) ===
# DATABASE_URL not needed for SQLite
# Database file will be created automatically as db.sqlite3t Variables for ESG Platform Backend
# Copy these to your Render dashboard under Environment Variables

# === CORE DJANGO SETTINGS ===
SECRET_KEY=%&e$ea5h=idf38c47rh*98dyupkdpb99bzei5ny3tmzzw84^uj^
DEBUG=False
DJANGO_SETTINGS_MODULE=scn_esg_platform.settings

# === HOST CONFIGURATION ===
ALLOWED_HOSTS=.onrender.com,localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=https://your-vercel-app.vercel.app

# === RENDER BUILD CONFIGURATION ===
# Build Command: cd backend && pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate
# Start Command: cd backend && gunicorn scn_esg_platform.wsgi:application --host 0.0.0.0 --port $PORT

# === DATABASE ===
# This will be auto-populated by Render PostgreSQL service
DATABASE_URL=postgresql://username:password@hostname:port/database_name

# === CORS & FRONTEND ===
CORS_ALLOW_ALL_ORIGINS=False
CORS_ALLOW_CREDENTIALS=True

# === AI SERVICES ===
GOOGLE_AI_API_KEY=your_google_ai_api_key_here

# === EMAIL CONFIGURATION ===
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=ESG Platform <your-email@gmail.com>

# === REDIS (for caching and background tasks) ===
REDIS_URL=redis://localhost:6379/0

# === SECURITY SETTINGS ===
SECURE_SSL_REDIRECT=True
SECURE_PROXY_SSL_HEADER=HTTP_X_FORWARDED_PROTO,https
SECURE_HSTS_SECONDS=31536000
SECURE_HSTS_INCLUDE_SUBDOMAINS=True
SECURE_HSTS_PRELOAD=True

# === FILE UPLOADS ===
MAX_UPLOAD_SIZE=52428800

# === RATE LIMITING ===
RATELIMIT_ENABLE=True

# === LOGGING ===
LOG_LEVEL=INFO

# === ENVIRONMENT ===
ENVIRONMENT=production
RENDER_ENVIRONMENT=production
