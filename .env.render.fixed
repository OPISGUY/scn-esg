# Render Environment Variables for ESG Platform Backend
# Copy these to your Render dashboard under Environment Variables

# === CORE DJANGO SETTINGS ===
SECRET_KEY=%&e$ea5h=idf38c47rh*98dyupkdpb99bzei5ny3tmzzw84^uj^
DEBUG=False
DJANGO_SETTINGS_MODULE=scn_esg_platform.settings

# === HOST CONFIGURATION ===
DJANGO_ALLOWED_HOSTS=.onrender.com,localhost,127.0.0.1,scn-esg-backend.onrender.com
CORS_ALLOWED_ORIGINS=https://scn-esg.vercel.app,https://scn-esg-platform-pc4ovhdpaa-opisguys-projects.vercel.app

# === RENDER BUILD CONFIGURATION ===
# Build Command: cd backend && pip install -r requirements.py313.txt && python manage.py collectstatic --noinput && python manage.py migrate && python create_test_users.py
# Start Command: cd backend && gunicorn scn_esg_platform.wsgi:application --bind 0.0.0.0:$PORT

# === DATABASE (SQLite - No external database needed) ===
# DATABASE_URL not needed for SQLite
# Database file will be created automatically as db.sqlite3

# === CORS & FRONTEND ===
CORS_ALLOW_ALL_ORIGINS=False
CORS_ALLOW_CREDENTIALS=True

# === AI SERVICES ===
GOOGLE_AI_API_KEY=your_google_ai_api_key_here

# === SECURITY SETTINGS ===
SECURE_SSL_REDIRECT=True
SECURE_PROXY_SSL_HEADER=HTTP_X_FORWARDED_PROTO,https

# === ENVIRONMENT ===
ENVIRONMENT=production
RENDER=true
